version: "2"

run:
  concurrency: 4
  modules-download-mode: readonly
  issues-exit-code: 1
  tests: true

linters:
  default: all
  exclusions:
    rules:
      - path: '(.+)_test\.go'
        linters:
          - govet
          - err113
          - errcheck
          - gosec
          - goconst
          - maintidx
          - dupl
      # warpdroid/node is the gomobile entry point. It returns errors
      # as strings (gomobile cannot bind Go's `error` type directly)
      # and reuses short literals like "false" / "client not
      # initialized" across exported wrappers, both of which the
      # repo-wide rules legitimately flag elsewhere. Skip the style
      # rules here while keeping correctness checks (staticcheck,
      # govet, etc.) enabled.
      - path: '^warpdroid/node/'
        linters:
          - err113
          - errcheck
          - goconst
  disable:
    - mnd
    - interfacebloat
    - iface
    - godot
    - godoclint
    - dupl
    - containedctx
    - contextcheck
    - copyloopvar
    - cyclop
    - depguard
    - dogsled
    - exhaustive
    - exhaustruct
    - forbidigo
    - forcetypeassert
    - funcorder
    - funlen
    - gochecknoglobals
    - gochecknoinits
    - gocognit
    - gocyclo
    - godox
    - goprintffuncname
    - inamedparam
    - intrange
    - ireturn
    - lll
    - musttag
    - nilnil
    - nlreturn
    - nonamedreturns
    - nolintlint
    - noinlineerr
    - paralleltest
    - perfsprint
    - revive
    - tagalign
    - tagliatelle
    - testpackage
    - varnamelen
    - wrapcheck
    - wsl
    - wsl_v5
    - unconvert
    - unused
    - testifylint

#formatters:
#  enable:
#    - gci # conflicts with goimports
#    - gofumpt # conflicts with Goland
