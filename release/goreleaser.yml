project_name: warpnet
version: 2
release:
  github:
    owner: Warp-net
    name: warpnet

builds: [ ]  # built already

archives:
  - id: member
    files:
      - dist/**

checksum:
  name_template: '{{ .ProjectName }}_checksums.txt'
  algorithm: sha256
  files:
    - dist/**

changelog:
  skip: true

