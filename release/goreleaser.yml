project_name: warpnet
version: 2

release:
  github:
    owner: Warp-net
    name: warpnet

builds: []

archives:
  - files:
      - dist/**

checksum:
  name_template: '{{ .ProjectName }}_checksums.txt'
  algorithm: sha256

# Если хочешь полностью отключить changelog:
changelog:
  disable: "true"
