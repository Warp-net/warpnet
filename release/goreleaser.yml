project_name: warpnet
version: 2

release:
  github:
    owner: Warp-net
    name: warpnet

builds:
  - skip: true

blobs:
  - glob: dist/*.tar.gz
  - glob: dist/*.zip

checksum:
  name_template: '{{ .ProjectName }}_checksums.txt'
  algorithm: sha256

changelog:
  disable: "true"
