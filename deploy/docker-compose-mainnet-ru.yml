services:
  push-gateway:
    container_name: push-gateway-mainnet
    image: prom/pushgateway:latest
    network_mode: host
    command:
      - "--web.listen-address=:4091"
    restart: always

  warpnet:
    container_name: warpnet-mainnet
    image: ghcr.io/warp-net/warpnet-bootstrap:latest
    network_mode: host
    restart: always
    environment:
      - NODE_PORT=4011
      - SOCKS5_PORT=4080
      - NODE_HOST_V4=130.94.88.38
      - NODE_NETWORK=warpnet
      - LOGGING_LEVEL=info
      - LOGGING_FORMAT=json
      - NODE_SEED=warpnet-ru
      - NODE_METRICS_GATEWAY=127.0.0.1:4091

